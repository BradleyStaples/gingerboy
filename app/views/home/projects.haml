.contents-wrapper.padding-large.centered
  %ul.no-list.margin-none.padding-none
    - @projects.each do |p|
      %li.clearfix.padding-none.project-container
        .project-entry
          .project-thumbnail-container
            %a.project-thumbnail-link{:href => image_path("projects/#{p.image}"), :target => '_blank', :title => p.name}
              %img.project-thumbnail.img-polaroid{:src => image_path("projects/thumbs/#{p.image}")}
          = link_to p.name, p.url, :target => "_blank", :class => 'text-dense project-title'
          %p.github-link.text-tiny
            %a{:href => p.github, :target => '_blank' } GitHub repo
          %p.clear-both
            #{p.description.html_safe}
        .project-divider.clear-both
