.contents-wrapper.padding-large.centered
  %ul.no-list.margin-none.padding-none
    - @portfolios.each do |p|
      %li.clearfix.padding-none.project-container
        .project-entry
          .thumbnail-container
            %a.fancybox{:href => image_path("portfolios/#{p.image}"), :target => '_blank', :title => p.name}
              %img.img-polaroid{:src => image_path("portfolios/thumbs/#{p.image}")}
          .text-dense.project-title
            = link_to p.name, p.url, :target => "_blank"
          %p.clear-both.text-small
            #{p.description.html_safe}
